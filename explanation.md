
# 주가 예측 스크립트 설명 (`stock_prediction.py`)

이 문서는 `stock_prediction.py` 스크립트의 작동 방식, 모델 성능을 향상시키기 위한 아이디어, 그리고 핵심 알고리즘인 랜덤 포레스트에 대해 설명합니다.

---

## 1. `stock_prediction.py` 동작 방식

스크립트는 총 6단계의 파이프라인으로 구성되어 주가를 예측합니다.

#### 1단계: 데이터 수집
- `yfinance` 라이브러리를 사용하여 특정 종목의 주가 데이터를 다운로드합니다.
- 현재 날짜 기준으로 1년 전까지의 '시가(Open)', '고가(High)', '저가(Low)', '종가(Close)', '거래량(Volume)' 데이터를 가져옵니다.

#### 2단계: 데이터 전처리 및 특성 엔지니어링
- **타겟 변수 생성**: 모델이 예측할 값인 '내일의 종가'를 `Target`이라는 새로운 컬럼으로 만듭니다. 이는 현재 날짜의 데이터를 사용하여 다음 날의 종가를 맞추도록 학습하기 위함입니다.
- **특성(Feature) 생성**: 모델이 주가의 패턴을 더 잘 학습할 수 있도록 추가적인 정보를 만듭니다.
  - `MA5`: 5일 이동 평균. 주가의 단기적인 추세를 나타냅니다.
  - `MA20`: 20일 이동 평균. 주가의 중장기적인 추세를 나타냅니다.

#### 3단계: 학습 및 테스트 데이터 분할
- **시계열 데이터 분할**: 주가와 같은 시계열 데이터는 시간의 흐름이 중요하므로, 데이터를 무작위로 섞지 않고 시간 순서에 따라 분할합니다.
  - 전체 데이터의 **앞부분 80%**를 모델 학습용(`X_train`, `y_train`)으로 사용합니다.
  - 나머지 **뒷부분 20%**를 모델 성능 평가용(`X_test`, `y_test`)으로 사용합니다.
- **데이터(X)와 정답(y) 분리**:
  - `X`에는 예측에 사용할 특성들('Close', 'High', 'Low', 'Open', 'Volume', 'MA5', 'MA20')이 포함됩니다.
  - `y`에는 예측할 대상인 `Target`('내일의 종가')이 포함됩니다.

#### 4단계: 모델 학습
- `scikit-learn` 라이브러리의 `RandomForestRegressor` 모델을 사용합니다. ('Regressor'는 연속된 숫자를 예측하는 회귀 모델을 의미합니다.)
- `fit` 메소드를 사용하여 준비된 학습용 데이터(`X_train`, `y_train`)를 모델에 주입하여 주가 예측 패턴을 학습시킵니다.

#### 5단계: 예측 및 평가
- **예측**: 학습된 모델을 사용하여, 본 적 없는 데이터인 테스트 데이터(`X_test`)에 대한 예측을 수행합니다.
- **평가**:
  - 모델의 예측값과 실제 정답(`y_test`)을 비교하여 모델의 성능을 평가합니다.
  - `MSE (평균 제곱 오차)`: 예측 오차의 크기를 나타내는 지표. 낮을수록 좋습니다.
  - `R-squared (R2 점수)`: 모델이 실제 데이터를 얼마나 잘 설명하는지를 나타내는 지표. 1에 가까울수록 좋습니다.
  - 실제 주가와 예측 주가를 그래프로 그려 시각적으로 확인하고, 이미지 파일로 저장합니다.

#### 6단계: 내일 주가 예측
- 학습된 모델에 가장 최근(어제)의 데이터를 입력하여, **다음 거래일의 종가**를 최종적으로 예측하고 출력합니다.

---

## 2. 추가적인 학습 데이터 (모델 성능 향상 방안)

현재 모델은 기본적인 주가 데이터와 이동 평균만을 사용하고 있습니다. 더 다양한 데이터를 추가하면 모델의 예측 성능을 높일 수 있습니다.

#### 1. 기술적 지표 추가
주가 차트 분석에 사용되는 다양한 기술적 지표들을 특성으로 추가할 수 있습니다.
- **RSI (Relative Strength Index)**: 현재 주가의 상승 혹은 하락 추세의 강도를 나타내어 과매수/과매도 상태를 파악합니다.
- **MACD (Moving Average Convergence Divergence)**: 두 이동 평균선 사이의 관계를 통해 추세의 강도와 방향, 전환점 등을 파악합니다.
- **볼린저 밴드 (Bollinger Bands)**: 주가의 변동성을 시각적으로 보여주는 밴드로, 주가가 밴드 상단이나 하단에 닿을 때를 분석에 활용합니다.
- **OBV (On-Balance Volume)**: 거래량은 주가에 선행한다는 개념을 바탕으로 거래량의 변화를 추적합니다.

#### 2. 외부 데이터 추가
주가에 영향을 미치는 외부 요인들을 데이터로 활용할 수 있습니다.
- **경제 지표**: 금리, 환율(특히 수출/수입 기업), 소비자 물가 지수(CPI) 등 거시 경제 지표.
- **시장 지수**: 코스피(KOSPI), 코스닥(KOSDAQ)과 같은 전체 시장의 움직임.
- **뉴스/소셜 데이터**: 해당 기업 관련 뉴스의 긍정/부정 감성 분석 결과나, 소셜 미디어 언급량 등을 수치화하여 사용.

#### 3. 하이퍼파라미터 튜닝
- `RandomForestRegressor` 모델을 생성할 때 설정하는 값들(예: `n_estimators`-트리 개수, `max_depth`-트리 깊이)을 '하이퍼파라미터'라고 합니다.
- `GridSearchCV`나 `RandomizedSearchCV` 같은 도구를 사용해 모델의 성능을 최적화하는 최상의 하이퍼파라미터 조합을 찾을 수 있습니다.

---

## 3. 랜덤 포레스트 (Random Forest)란?

랜덤 포레스트는 **앙상블(Ensemble) 학습** 방법의 한 종류로, 이름 그대로 '무작위로 만들어진 숲'을 의미합니다.

#### 핵심 개념
- **"나무(Tree)들이 모여 숲(Forest)을 이룬다"**: 하나의 강력한 모델 대신, 여러 개의 간단한 모델(결정 트리, Decision Tree)을 만들어 그 결과를 종합하여 최종 예측을 하는 방식입니다.
- **"집단 지성은 개인보다 낫다"**: 여러 명의 전문가가 함께 의견을 모으면 한 명의 전문가보다 더 나은 결정을 내릴 수 있다는 아이디어와 유사합니다.

#### 동작 원리
1.  **데이터 샘플링**: 원본 데이터에서 무작위로 일부 데이터를 선택합니다. 이 과정을 여러 번 반복하여 여러 개의 데이터 셋을 만듭니다. (부트스트랩 샘플링)
2.  **특성(Feature) 무작위 선택**: 각 데이터 셋으로 결정 트리를 학습시킬 때, 전체 특성 중 일부 특성만을 무작위로 선택하여 사용합니다.
3.  **결정 트리(Decision Tree) 생성**: 위 과정을 통해, 각각 다른 데이터와 다른 특성으로 학습된 **다양한 모습의 결정 트리**들이 여러 개 만들어집니다.
4.  **결과 종합**:
    - **회귀 (Regression)** 문제 (주가 예측 등): 모든 트리가 예측한 값들의 **평균**을 최종 예측값으로 사용합니다.
    - **분류 (Classification)** 문제 (스팸 메일 분류 등): 모든 트리가 예측한 결과 중 가장 많이 나온 결과(다수결)를 최종 예측값으로 선택합니다.

#### 장점
- **높은 정확도**: 여러 개의 모델을 종합하므로 일반적으로 예측 성능이 매우 우수합니다.
- **과적합(Overfitting) 방지**: 각 트리가 서로 다른 데이터와 특성으로 학습되므로, 모델이 학습 데이터에만 과도하게 최적화되는 과적합 현상을 효과적으로 방지합니다.
- **특성 중요도 제공**: 어떤 특성(예: '거래량', '20일 이동평균')이 예측에 더 큰 영향을 미쳤는지 알 수 있어, 모델을 분석하고 이해하는 데 도움이 됩니다.

#### 단점
- **해석의 어려움**: 수백 개의 트리가 결합되어 있어, 모델이 왜 특정 예측을 했는지 그 내부 과정을 직관적으로 이해하기는 어렵습니다.
- **상대적으로 느린 속도**: 여러 개의 트리를 만들고 결과를 종합해야 하므로, 단일 모델에 비해 학습 및 예측 속도가 느립니다.
